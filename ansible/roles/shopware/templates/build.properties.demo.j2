# Hostname e.g. example.com
app.host = shopware.{{ ansible_fqdn }}
# Path e.g. /shopware. Leave blank if installed in document root
app.path =

# Database credentials
db.name = {{ shopware_db_name }}
db.host = {{ shopware_db_host }}
db.user = {{ shopware_db_username }}
db.password = {{ shopware_db_password }}
db.port = {{ shopware_db_port }}

test.dir  = ${basedir}/tests
test.dir.shopware = ${test.dir}

build.dir = ${basedir}/build
log.dir   = ${build.dir}/logs
sql.dir   = ${basedir}/_sql
cache.dir = ${basedir}/var/cache
app.dir   = ${basedir}/engine/Shopware

karma.config = ${basedir}/themes/Frontend/Responsive/karma.conf.js

script.php      = php
script.phpcs    = phpcs
script.phpmd    = phpmd
script.phpcpd   = phpcpd
script.phploc   = phploc
script.pdepend  = pdepend
script.phpcb    = phpcb
script.mysql    = mysql
script.phpunit  = ${basedir}/vendor/bin/phpunit
script.behat    = ${basedir}/vendor/bin/behat
script.karma    = karma

script.server.start = ${build.dir}/server/start_server
script.server.stop  = ${build.dir}/server/stop_server

demo.data = ${sql.dir}/demo/latest.sql